
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents recursively
    // WARNING: This is for development only. Secure this before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
